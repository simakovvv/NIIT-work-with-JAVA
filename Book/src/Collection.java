import java.util.ArrayList;
import java.util.List;

public class Collection {
    List<Person> personList = new ArrayList<>();

    personList.add();

    personLisr.forEach(System.out::println);
    java.util.Collection.reverse(personList);
}
